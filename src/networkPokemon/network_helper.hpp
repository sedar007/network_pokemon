#pragma once
/*
namespace pokemon {

    class network_helper {
    public:
        network_helper() = default;
        ~network_helper() = delete;

        template <typename T, typename PacketType>
        requires Packetable<T, PacketType>
        void send_list(std::shared_ptr<sockpp::tcp_socket> socket, const std::vector<T>& items);
    private:
    };
}



#include "network_helper.inl"
*/
